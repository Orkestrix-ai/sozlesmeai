# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Basic Working Rule
- **Follow the rules:** ALWAYS read the existing files before performing any operation.
- **Ask if you are unsure:** Never guess, stop the operation, or ask a question directly in the face of unclear or incomplete information. - **Do not make assumptions:** Do not guess code, file paths, or variables.

## Project

AI-powered **contract SaaS**: the user describes their needs in natural language, the system asks for missing information, generates an editable contract draft, converts it to PDF, and archives it.

Two documents are the source of this project and **precede the code**:

- **`design.md`** (English) — Design system. Color tokens, typography, landing sections, the dashboard, contract screen, admin panel, button/status system, responsive rules, design QA list in §12. Ready to implement and binding. Originally authored in Turkish; translated to English in place — the section numbering (§1…§13) is unchanged, so every `design.md §N` cross-reference elsewhere in this file still resolves.
- **`prd.md`** (English) — Product requirements. **Cut off in FR-08 (PDF)**: No written requirements for archiving, sharing, credit accounting, and admin panel; only mentioned in the §3 MVP scope list. Ask the user before making assumptions in these areas.

## Commands

```bash
npm run dev # development server (Turbopack)
npm run build # production build + TypeScript check
npm run lint # ESLint (entire project)
npm run start # run compiled application

npm run check:i18n # tr/en message parity + ICU token parity

npx eslint src/components/landing/hero.tsx # single file
npx tsc --noEmit # type check only
```

The test infrastructure is not yet established (it was outside the scope of Phase 1). `npm run build` is currently the only gateway for type safety.

## Architecture

**Next.js 16.3.4 + React 19 + TypeScript + Tailwind v4 + shadcn/ui (Radix) + next-intl.**

Next.js 16 is different from your tutorial. Read the relevant guide under `node_modules/next/dist/docs/` before writing code. Here are some things it has bitten off on:

- **`middleware.ts` is now `proxy.ts`.** The file is `src/proxy.ts`, export name is `proxy` or default. The function is the same.
- **`params` is a Promise** — `const { locale } = await params`.
- **`PageProps<'/[locale]'>` and `LayoutProps<'/[locale]'>` are global type helpers; don't write your own props interface. - Readable without locale prop-drilling via `next/root-params` (in Server Components).

### Folders

```
src/app/[locale]/ # root layout here (html/body), all routes under
src/app/[locale]/style-guide/ # internal design reference, non-i18n
src/components/landing/ # landing sections, each a separate component
src/components/ui/ # primitives (button, status-badge) according to design.md §10
src/i18n/ # routing, navigation, request configuration
src/proxy.ts # locale detection (formerly middleware)
messages/{tr,en}.json # ALL visible text
```

### i18n

- Languages: `tr` (default) and `en`. Configuration in one place: `src/i18n/routing.ts`.
- **No visible strings are embedded in the component** — all are in `messages/*.json`. The only exception is the `style-guide` page (internal developer reference).
- In-app links, use `Link` / `redirect` / `useRouter` in `@/i18n/navigation` NOT `next/link`; otherwise, the language prefix will be lost. - Update **both files** when adding new text; missing key is a runtime error in production. Two gates now enforce this, and they cover different halves:
  - **`tsc`** — `global.d.ts` augments next-intl's `AppConfig` with `Messages: typeof trMessages`, so every `t("...")` / `t.rich("...")` key is checked against `messages/tr.json`. Deleting or renaming a key is a *build* error, not a runtime one. `tr.json` is the schema source (default locale, TR-first copy). `Locale` is deliberately left un-narrowed — narrowing it would force a cast at every `setRequestLocale(locale)` for no gain, since `[locale]/layout.tsx` already validates with `hasLocale`.
  - **`npm run check:i18n`** (`scripts/check-i18n.mjs`) — what `tsc` cannot see: keys present in one locale file but not the other, and `{arg}` / `<tag>` token mismatches between them. A `t.rich` key whose `<link>` is missing in one locale throws at runtime.
- Values that *carry* a message key (validation results, error codes) are typed against that schema in `src/lib/i18n-keys.ts` (`ValidationKey`, `AuthErrorKey`, `AppErrorKey`) — that's why `FieldErrors` is `Record<string, ValidationKey>` and `authErrorKey()` doesn't return a bare `string`.
- When a key list is built from an array and used in a template literal, store the **full path** (`"plans.starter.features.f1"`), not the leaf (`"f1"`). Destructuring in `.map` decorrelates the plan key from its feature keys, so `` t(`plans.${key}.features.${featureKey}`) `` type-checks as a cross-product and invents paths that don't exist — see the comment on `PLANS` in `src/components/landing/pricing-section.tsx`.
- **Dev-server gotcha:** deleting or renaming a key while `next dev` is running can leave a ~1s window where the JSON module has hot-updated but the component chunk has not, producing a `MISSING_MESSAGE` for a key the source no longer references (the stack frame shows the *new* source asking for the *old* key). If that happens, restart the dev server before believing it — `tsc` is the authority.

### LLM providers

`src/app/api/contracts/[id]/turn` and `/review` never touch `@anthropic-ai/sdk` or `groq-sdk` directly — both call `getLlmProvider()` from `src/lib/ai/provider/` (selected by `LLM_PROVIDER` env var, default `"anthropic"`), which returns a `streamTurn`/`createMessage` interface (`src/lib/ai/provider/types.ts`) implemented by `provider/anthropic.ts` (`claude-opus-5`, adaptive thinking, prompt caching) and `provider/groq.ts` (`openai/gpt-oss-120b`, no prompt caching, per-tool-call `role:"tool"` messages instead of Anthropic's single `tool_result` batch). Tool schemas in `contract-tools.ts`/`review-tool.ts` are provider-neutral (`LlmToolDef`) and shared verbatim between both. Anthropic's `tool_choice` can't be forced while `thinking` is on (400 if you try) — the `forceTool` request field is honored by Groq only, Anthropic ignores it. Both provider clients are constructed lazily (first call, not module import) so an unset key for the *inactive* provider doesn't crash the app at boot.

### Phase Plan

All five phases are code-complete:

- **Phase 1** — skeleton, design system, bilingual landing page.
- **Phase 2** — Supabase auth, workspace/membership scheme, RLS, dashboard shell and dashboard (design.md §7).
- **Phase 3** — Contract creation screen (design.md §8) + Claude API (`claude-opus-5`, adaptive thinking, streaming); PRD FR-02…FR-07.
- **Phase 4** — PDF (Puppeteer/Chromium, not react-pdf — see below), archive, sharing (PRD FR-08), credit accounting.
- **Phase 5** — Admin panel (design.md §9).

**Standing caveat (Anthropic path only):** `ANTHROPIC_API_KEY` in `.env` has stayed a placeholder throughout, so the Anthropic provider (`provider/anthropic.ts`) has never been exercised against the real Anthropic API in this environment. The Groq provider (`provider/groq.ts`), added 2026-09-08, HAS been live-verified directly against the Groq API — a real `GROQ_API_KEY` confirmed tool-calling, streaming, strict nested-object schemas, and Turkish output quality against `openai/gpt-oss-120b`, using the app's actual tool schemas byte-for-byte. Everything else (auth, workspace, DB writes, PDF generation, sharing, admin) has been live-verified end-to-end via manual/manual-equivalent paths regardless of LLM provider. If odd behavior shows up in the chat/review flow specifically on the Anthropic path once a real `ANTHROPIC_API_KEY` is in place, start there — the two providers share tool schemas and prompts but have independent request/response mapping code.

**PDF library:** `@react-pdf/renderer`/`fontkit` has a confirmed upstream bug rendering Turkish dotless-ı (U+0131) as the digit "1" (reproduced across three font families). Switched to Puppeteer: render HTML with embedded `@fontsource` fonts via headless Chromium (`src/lib/pdf/contract-html.ts` + `render.ts`), which shapes text correctly since it uses Chromium's own engine, not fontkit.

## Design guidelines (non-negotiable)

Essence of design.md §1, §3, §5, and §12. These are priorities when writing a screen:

- **Red is not a decorative color, but an indicator of action and importance.** Overall ratio: 60% light neutral / 25% dark / 10% gray / **5% red**. Do not expose the user to multiple red CTAs simultaneously.
- **Each screen has only one primary action.**
- **Forbidden visual language:** purple-blue neon "AI" palette, gradients, neon glow, glass effects, overly rounded toy cards, robot/brain/magic wand images, 3D illustrations that do not support the content.
- **Border is dominant over shadow.** Single shadow `shadow-card`; radius 8–12 px (`--radius: 10px`), 16–20 px only in large marketing areas. - Icons are line-based and have the same stroke weight (`lucide-react`).
- Do not write text that includes legal guarantees or claims of a "completely error-free contract".

When you finish a screen, go through the **QA list in design.md §12** one by one.

### Dark surfaces are NOT dark mode

The `ink-950` / `ink-900` fields in design.md (navbar, hero, sidebar, final CTA, left panel of the contract screen) are **brand surfaces** that live inside the open theme. Write them with open utilities like `bg-ink-950`; not with `dark:`.

The line `@custom-variant dark (&:is(.dark *))` in `globals.css` is intentionally left: it binds the `dark:` variant to a `.dark` class that is never added, so `dark:` classes from shadcn components remain dead. **Do not delete** — if deleted, Tailwind v4 falls back to `prefers-color-scheme` and the palette silently breaks for visitors whose OS is in dark mode.

Tailwind's built-in `stone` palette is also deliberately cleared (`--color-stone-*: initial`). Writing an off-palette shade (e.g. `stone-500`) then generates no class at all — visibly broken is preferred over silently wrong.

## Billing model — prepaid credits, charged at the PDF

There are **no packages and no subscription.** The model, decided 2026-09-10 and applied in `supabase/migrations/20260910120000_pay_as_you_go.sql`, is a prepaid wallet:

- **1 credit = 1 contract**, and the deduction happens **only when the PDF is generated** (`operation_costs.pdf_generate = 1`). Chatting, drafting, AI editing, manual editing and the risk check are all **0** — they still write an `amount = 0` ledger row for audit continuity, they just don't move the balance.
- **A contract is charged once, ever.** `pdf/route.ts` passes a fixed `pdf:<contract_id>` idempotency key, so `consume_credits`' idempotency short-circuit makes every later PDF of that contract free. This rule lives in the **database**, not in route logic — don't "optimize" that key back into a `randomUUID()`.
- **The `can_afford` gate on `/turn` and `/review` asks about `pdf_generate`, not about the operation being performed.** Those operations cost 0, so gating on them would leave the door permanently open and let a zero-balance account burn unlimited provider tokens. The gate means "will the user be able to pay for the output", not "what does this turn cost". Both routes carry a comment saying so; keep it.
- **Signup grants 1 credit, and only to the personal workspace** (`tg_workspace_add_owner_membership`). Team workspaces start at 0 — before this, every workspace insert minted credits, so the workspace switcher was a free-credit button.
- Ledger `reason` for that grant is now `signup_grant`. `signup_starter_grant` still appears in `KNOWN_REASON_KEYS` and in the message files because `credit_ledger` is append-only and historical rows can never be rewritten.
- There is **one dashboard for everyone** (`src/components/dashboard/workspace-overview.tsx`). The old `starter-overview` / `pro-overview` / `business-overview` trio is gone; the team + activity cards it used to own now render conditionally on `workspace.isPersonal === false`. The split is workspace type, not a purchased tier.

## Product principles

- **AI output is never shown as automatically "final".** Draft / review / approved states are visually distinct (design.md §8). Every output is editable and subject to user approval.
- Unknown information is **never fabricated**; missing/ambiguous fields are marked explicitly (PRD FR-04).
- The risk/consistency check is not legal advice or a validity guarantee (PRD FR-07).
- **Out of MVP scope:** electronic signatures, secure-link signing, signature-status tracking, signed-document archive, an advanced client portal. Don't present these as available features in primary navigation (design.md §7.1); mark them "coming soon" only where needed.

## Database security

Supabase/PostgREST, no `DATABASE_URL`, no ORM — every DB access goes through `@supabase/supabase-js` (`.from()`/`.rpc()`), parametric by construction. Five-column hardening written 2026-09-08 (`supabase/migrations/20260908*`) and **applied to the live project 2026-09-10**; `security_assertions.sql` passes and `src/lib/supabase/types.ts` needed no regeneration (it had been written ahead of the apply and already matched). The two-day gap was not free: the app calls `can_afford` / `create_contract_version` / `get_shared_contract`, none of which existed in the database yet, so the contract chat failed on every message with a generic "Mesaj gönderilemedi" — the client (`src/components/contracts/contract-workspace.tsx`) collapses any non-2xx into that one string, so the real cause was only visible in the DB. **Writing a migration file is not applying it** — after any new migration, apply it, then re-run `supabase/checks/security_assertions.sql`. Note also that remote migration versions do **not** match the repo filenames (all 22 were applied via Supabase MCP, which stamps its own timestamp), so `supabase db push` would try to re-apply everything — MCP/dashboard is this project's migration path:

- **Tenant isolation.** RLS is on for every `public` table; `private.*` SECURITY DEFINER helpers (`workspace_ids_for_current_user`, `current_workspace_role`, `is_workspace_admin`, `coworker_ids`, `is_platform_admin`) break the `workspace_members` RLS-recursion trap (42P17) — never inline a policy that queries its own table, add a helper in `private` instead. `contract_documents`/`contract_findings` carry a **composite FK** to `(contract_versions.id, contract_id)`, not just `version_id` — this is what stops a forged `contract_id` + someone-else's `version_id` pairing. `contract_documents.storage_path` is **not client-writable in practice**: a `BEFORE INSERT/UPDATE` trigger (`tg_contract_documents_normalize_path`) recomputes it from `contract_id`/`version_no` every time, so whatever the client sends is overwritten — this is what makes the public `/s/[token]` share page's `storage_path`-based signed URL trustworthy.
- **Least privilege.** There is no `app_user`/`migration_user` pair to create — this project already maps 1:1 onto Supabase's own roles: `authenticated`/`anon` (PostgREST JWT roles, no DDL, table/RPC grants + RLS only) = the CRUD role; `postgres` (Supabase MCP/CLI only, never touched by application code) = the migration role; `service_role` (bypasses RLS entirely) = the one role to keep rare — after this hardening it's used in exactly one place, `src/lib/supabase/admin.ts`, called from exactly one call site (`s/[token]/page.tsx`, for `createSignedUrl` only; the share page's actual row lookups now go through the `get_shared_contract` RPC on the normal client). `eslint.config.mjs` enforces this with `no-restricted-imports` on `@/lib/supabase/admin` — new call sites must be justified, not just added. `alter default privileges` closes new tables/sequences/functions to `anon`/`authenticated` by default (previously each migration had to remember its own `revoke`).
- **Append-only audit tables.** `credit_ledger`, `workspace_activity`, `admin_audit_log` reject `UPDATE`/`DELETE`/`TRUNCATE` at the trigger level (`private.tg_append_only`, statement-level, fires for `service_role` too — only a superuser session-replication-role bypass gets around it, which this project's connections never have). Consequence: these tables' `actor_id`/`contract_id`/`workspace_id` are **plain `uuid` columns, not FKs** to anything cascadable — a deleted contract or profile never rewrites a historical ledger row (`workspace_activity`'s `actor_id → profiles` FK is the one exception, kept `on delete restrict` for the `profiles(full_name)` PostgREST embed in `getWorkspaceActivity()`). Run `supabase/checks/security_assertions.sql` after any migration that touches these three tables or adds a new SECURITY DEFINER function.
- **Credit integrity.** `workspace_credits.balance >= 0` is a DB-level `CHECK`, not just application logic. `credit_ledger.idempotency_key` (unique, nullable) makes `consume_credits`/`refund_credits`/`admin_add_credits` safe to retry. `contract_versions` has **no direct INSERT grant** for `authenticated` — the only way to create a version is `create_contract_version(contract_id, sections, source, idempotency_key)`, which locks the parent `contracts` row, computes `version_no`, deducts credit, and inserts the version in one transaction (no more "credit spent, version write failed" gap). `can_afford(workspace_id, operation)` is a cheap pre-flight gate called *before* the LLM/PDF work starts, purely to stop burning provider tokens on a workspace that's already at zero — the real, authoritative deduction is still `consume_credits` inside the RPC. Zero-cost operations (everything except `pdf_generate`, the only charged one — see "Billing model" above) still write a `credit_ledger` row (`amount = 0`) for audit continuity; they just don't move the balance. Note also that `refund_credits` matches the consume row on `workspace_id` **and** the idempotency key: the key-only lookup it shipped with let an admin/editor of one workspace mint a refund into their own workspace from another workspace's key (fixed 2026-09-10).

  **Don't write `tg_credit_ledger_apply()` as a single upsert again.** It used to materialize the balance with `insert ... values (workspace_id, new.amount) on conflict do update set balance = balance + excluded.balance`. Postgres evaluates CHECK constraints on the **proposed insert tuple**, before conflict resolution — `ON CONFLICT` only catches unique/exclusion violations — so once `workspace_credits_balance_nonneg` went live (2026-09-10), every negative `amount` was tested as the tuple `(workspace_id, -1)` and raised 23514, failing *every* paid operation even on a workspace with credits to spare. The trigger now ensures the row (`values (workspace_id, 0) on conflict do nothing`) and then `UPDATE`s it, so the CHECK is evaluated on the result. A real overdraw still raises 23514, but nothing reaches it: `consume_credits` raises `insufficient_credits` (P0001 → HTTP 402) first.
- **Error handling.** `src/lib/db/errors.ts` (`toPublicError`, `DbError`) and `src/lib/db/safe.ts` (`dbQuery`, `dbRpc`, `withApiErrors`) are the *only* place a Postgres/PostgREST error should be translated for a client response — never `error.message` directly. Classification is by SQLSTATE (`PG_CODE_MAP`) plus a narrow allowlist of this project's own `raise exception '<label>'` strings (`MESSAGE_ALLOWLIST`); anything outside that allowlist is logged server-side with a short `reference` code and returned to the client as `generic` — extend the allowlist when a new RPC introduces a new `raise exception` label, never by matching on Postgres's own generated text.
- **Input validation at the DB boundary.** `src/lib/db/schemas.ts` is a second, narrower Zod layer than `src/lib/validation.ts` — the latter is form/UX validation returning i18n keys (deliberately not Zod, see its header comment); `db/schemas.ts` is what a route/action parses a route param, header, or RPC argument through immediately before it reaches Supabase.

## Open items

- **The money price is still a placeholder — the credit costs no longer are.** `operation_costs` now carries real product decisions (`pdf_generate = 1`, everything else `0`, all `is_placeholder = false`). What remains undecided is what **one credit costs in money**: `pricing.payg.price` in `messages/*.json` ships as ₺249 / $9, and the two are not a conversion of each other. Changing them is a message-file edit; changing the credit cost of an operation is a new migration.
- **There is no way for a user to buy credits.** No payment provider is integrated, so `admin_add_credits` (admin panel → `/admin/billing`) is the only path by which a balance ever goes up after the 1-credit signup grant. `dashboard.credits.topUpSoon` says this plainly on `/settings` rather than rendering a button that does nothing. A provider integration also means updating the Privacy sub-processor article and the Terms fee article.
- **A zero-balance user is stopped at the door, and the UI doesn't explain why.** `/turn` and `/review` return 402 `insufficient_credits`, but `contract-workspace.tsx` collapses every non-2xx into one boolean (`:160`, `:187`), so the user sees a generic error instead of "you're out of credits". Worth fixing when the top-up path exists — surfacing "out of credits" with no way to buy any would be worse.
- **`activity_kind.plan_changed` is a dead enum value.** Nothing writes it and nothing can. It was left in place deliberately: removing an enum value means recreating the type and converting a column on `workspace_activity`, which is append-only. The message key stays too, because `ActivityKind` is a closed union and the `kinds.${item.kind}` lookup must resolve for every member.
- **`src/components/dashboard/usage-meter.tsx` was deleted** along with the monthly-allowance concept; `src/components/ui/progress.tsx` survives (used by `/style-guide`) but currently has no screen consumer.
- **PDF failure compensation has one bounded leak.** `pdf/route.ts` consumes the credit after the signed URL is obtained, so only the `contract_documents` upsert branch still needs `refund_credits`. After that refund the `pdf:<contract_id>` key is burnt in the ledger, so the retry is free. Closing it means moving the document-row write into the same transaction as the consume (i.e. an RPC, the way `create_contract_version` does it).
- The footer's corporate links (About, Contact) still point to in-page sections, not real pages — `HREFS` in `src/components/landing/site-footer.tsx`. Privacy and Terms now resolve to real routes (`src/app/[locale]/(legal)/{terms,privacy}/page.tsx`); `HREFS` renders `#`-prefixed entries as plain `<a>` and everything else through `@/i18n/navigation`'s `Link`.
- **The legal documents are written; only the company identity is still blank.** `/terms` and `/privacy` render 15 articles each from `src/components/legal/documents.ts` (declarative article/block spec, full message paths per the `PLANS` rule above) through `src/components/legal/legal-document.tsx`. Text lives under the `legal` namespace in `messages/*.json`, written against what the code actually does — AI providers named as sub-processors, EU (Ireland) hosting, non-expiring share links that always serve the *latest* version, archive-is-not-deletion, and append-only ledgers that cannot be erased. Two consequences worth keeping true: **(1)** `legal.entity.*` is the single fill-in point — every field except `tradeName` (`"SözleşmeAI"`) ships as `""`, and `LegalDocument`'s `dl` block skips empty rows, so filling them in is a one-file edit with no text rewrite. `legalName` is deliberately empty: SözleşmeAI is the trading name, and no registered company name exists yet — don't put a brand there to fill the gap. This is also why `ENTITY_CONTACT` in `documents.ts` lists **both** `legalName` and `tradeName`: with every other field blank, dropping `tradeName` would empty the whole `dl`, and the closing article's "contact details are below" sentence would render with nothing under it. Until at least `address` and `email` are real, the KVKK art. 10 disclosure is **incomplete** — a launch blocker, not a polish item. **(2)** the text asserts factual things about the system; changing share-link pinning/expiry, adding analytics or a payment provider, switching LLM provider, or adding a deletion path means updating the matching article. Article ids are locale-independent anchors (`#sharing`, `#ai-providers`), so deep links survive a language switch.
- **Brand name lives in four places, not one.** The product is **SözleşmeAI** (both locales — it's a proper noun, so `nav.brand` carries the same value in `tr.json` and `en.json`). The header wordmark, footer wordmark and `© {year}` line all read that one key, and so does the public share page (`s/[token]/page.tsx`). Two more copies are hardcoded because they have no translation context: `src/lib/ai/prompts.ts` (`RULES_TR`/`RULES_EN` name the product to the model) and `src/lib/pdf/contract-html.ts`'s `buildContractFooterTemplate()` (a parameterless pure function). A rename means editing all four — plus `legal.entity.tradeName` in both message files, which is the same string in a legal role rather than a wordmark. The earlier names **Cntrsign** and **agreefork** (the latter was the npm package and GitHub repo name) are retired; the repo is now `Orkestrix-ai/sozlesmeai` and the package is `sozlesmeai`. The old `Cntrsign/` kit and its `public/` copies were removed with the rename; the single brand asset now is `brand/sozlesmeai-logo.png`, the **source lockup** (chevron-S mark + wordmark + "Akıllı Sözleşme Çözümleri" tagline, white-on-black). It is a reference file, **not** served — nothing loads it at runtime. The mark was vectorized out of it into `src/components/brand/logo-mark.tsx` (two closed polygons, no holes, `fill="currentColor"`), and `src/components/brand/brand-lockup.tsx` pairs it with the `nav.brand` wordmark: sizing is in `em` (mark `1.5em`, gap `0.38em`) so the lockup keeps the source's proportions — mark height = 2.08 × the "S" cap height — at whatever font-size the caller sets. The lockup is currently used in **the navbar only** (`site-header.tsx`); the footer, sidebar, mobile drawer, auth shell and share page still render the plain text wordmark, and each is a one-line swap. Two deliberate deviations from the source file: the tagline is dropped (e-signature-adjacent marketing copy has no place in app chrome, and it is illegible at 17px), and the red "AI" uses palette `brand-red-500` rather than the source's `#FB021A`, which is not a design.md §2 color. `src/app/favicon.ico` is still the Next.js default — `LogoMark` is what an icon export should be built from. Note also that `buildContractFooterTemplate()` is rendered by Puppeteer *outside* the page and does **not** inherit the embedded `@fontsource` faces — it falls back to the host's `sans-serif`, so the `ş`/`ö` in the PDF footer wordmark depend on the deploy image carrying a Turkish-capable default sans.
- **Signup consent is recorded in `auth.users.raw_user_meta_data`, not in a table.** Signup carries one checkbox per legal document (`src/components/ui/checkbox.tsx` + `checkbox-field.tsx`), each validated separately, so `signupAction` writes `terms_accepted_at` and `privacy_accepted_at` as two independent acknowledgements alongside `full_name`/`locale` (same ISO value only because they arrive in one request). `handle_new_user` doesn't copy them into `profiles`, and a user can rewrite their own metadata via `auth.updateUser({ data })` — this is a record trail, not audit-grade proof. Promoting it to `profiles` columns (or an append-only consent table) needs a migration + `handle_new_user` change.
- **`src/components/ui/radio-group.tsx` is a primitive no screen currently uses.** It was built for the consent step before that moved to checkboxes; it stays in the UI kit and is documented in `/style-guide`, and `field.tsx`'s `asGroup` prop (label as a `<p>` wired by `aria-labelledby` instead of `<label htmlFor>`, since a `div[role="radiogroup"]` isn't labelable) exists for it. `CheckboxField` lives in its own file rather than `field.tsx` because it needs a click handler — `field.tsx` must stay server-safe for `/style-guide`.
- Email sending has no configured provider, so sharing is link-based only ("copy link"); email delivery is marked "coming soon" in `messages/*.json`.
- `prd.md` cuts off at FR-08; archive, sharing, credit accounting, and the admin panel followed design.md §7/§9 and the MVP scope list instead of a written FR — noted here since there's no PRD text to cross-check against.
- See the Phase Plan section above for the standing Phase 3 live-AI-verification caveat.
- **No error monitoring is configured** (no Sentry/Datadog/etc. in `package.json`). The 404/error pages (`src/app/[locale]/{not-found,error}.tsx`, `src/app/global-{not-found,error}.tsx`, `src/app/[locale]/(app)/{not-found,error}.tsx`, shared presentation in `src/components/feedback/status-view.tsx`) deliberately avoid claiming "our team has been notified" (`messages/*.json` → `errors.page.unexpected.description`) since that would be false. Update that copy once monitoring exists.
- **No support channel exists yet** (no support email/page). The error pages' secondary action is "go back" (`src/components/feedback/back-button.tsx`) rather than a support link; once a real support address is defined, a `mailto:` tertiary link embedding the reference code could be added (pairs with the footer `HREFS` open item above).
- **No search backend exists** (not even a filter on `/archive`). The error pages offer locale-aware quick links instead of a search box; if `/archive` gains a text filter, the 404 pages could grow a search box that feeds it via `?q=`.
- **Architectural note on `[locale]`:** it's a single dynamic segment, not a catch-all — `/tr/<unmatched>` (two+ segments, no matching `page.tsx`) never reaches `[locale]/layout.tsx` at all and falls straight to `src/app/global-not-found.tsx` (requires `next.config.ts`'s `experimental.globalNotFound: true`, confirmed available in Next 16.3.4). `src/app/[locale]/not-found.tsx` only fires for an explicit `notFound()` call from a matched route with no closer boundary (currently none call it — `(auth)/*` and `s/[token]` handle invalid state inline) or a single bad-locale segment like `/xx` — though in practice `src/proxy.ts`'s next-intl middleware redirects `/xx` → `/tr/xx` before that can happen, routing it to global-not-found instead. `global-not-found.tsx` resolves its locale from the `x-next-intl-locale` header set by that same middleware (falling back to the `NEXT_LOCALE` cookie, then `Accept-Language`) since it has no `params` to read.
- **Share links are not pinned to the version live at share time.** `/s/[token]` (via `get_shared_contract`) always serves the contract's *current* latest version — an edit made after sharing is visible on the existing link immediately. This was a deliberate scope decision during the 2026-09-08 DB security pass (pinning is a product call about whether a share link is a "live document" or a "frozen snapshot", and changes the share UX copy); only the security half was done — the link stops working once `contracts.status` leaves `ready`/`shared`. Pinning would mean adding `contract_shares.version_id` and reading that fixed version instead of "latest".
- **No user-deletion (GDPR) path.** Already impossible in the schema before the 2026-09-08 hardening (`workspaces.owner_id` is `on delete restrict`, every user owns a personal workspace) — the append-only audit trigger added in that pass doesn't change this, just makes it more permanent. Needs a real design (anonymize vs. hard-block) before FR work starts here.
- **Supabase Auth URL configuration is not tracked in this repo.** There is no `supabase/config.toml` (only `migrations/` and `checks/`), so **Site URL** and the **Redirect URLs** allow-list live only in the hosted dashboard, and the MCP Supabase tools have no auth-config write endpoint — dashboard only. This bit once: Site URL was set to a bare hostname *without a scheme*, and no app origin was in the allow-list, so GoTrue silently dropped the `emailRedirectTo` sent by `src/actions/auth.ts` and fell back to that schemeless Site URL. A schemeless `Location` is not an absolute URL — the browser resolved it as a *relative path* against `https://<ref>.supabase.co`, and every confirmation link died on the Supabase API gateway's `{"error":"requested path is invalid"}`. Two mitigations are now in code: `getBaseUrl()` prefers `NEXT_PUBLIC_SITE_URL` over the `Host` header, and `src/app/auth/confirm/route.ts` accepts **both** the stock `{{ .ConfirmationURL }}` template (`?code=` → `exchangeCodeForSession`) and a `{{ .TokenHash }}` template (`?token_hash=&type=` → `verifyOtp`), plus GoTrue's own `?error=&error_code=` failure redirect. Neither removes the config requirement. **Current state: there is no production domain** — the intended name is `sozlesmeai` (matching the brand and the repo), but **it has not been registered**, so Site URL and Redirect URLs are both on `http://localhost:3000` (`http://localhost:3000/**` in the allow-list; the `/**` wildcard is required because `emailRedirectTo` always carries a `?next=` query). This drifted once and bit exactly as described above: on 2026-09-10 a live probe (`GET /auth/v1/verify` with a junk token, reading the `Location` header) showed Site URL had been set to a schemeless `evrakflow.com`, so GoTrue answered `Location: /evrakflow.com#error=…`. `evrakflow.com` is not a domain this project uses and was reset to `http://localhost:3000`. That probe is the cheap way to read Site URL back — the dashboard is the only *write* path, but the value is observable from any failed verify. When a domain is chosen, **three places must change together**: Supabase Site URL, Supabase Redirect URLs (`<origin>/**`), and `NEXT_PUBLIC_SITE_URL` in the hosting env. Miss one and GoTrue falls back to Site URL without raising an error. Separately, the stock template forces the link to be opened in the browser that started signup (the PKCE code verifier cookie lives there) — switching the templates to `{{ .TokenHash }}` removes that constraint, at the cost of hardcoding the locale in the template's `next` param.
- **Email confirmation is turned OFF in the hosted project** (Authentication → Sign In / Providers → Email → "Confirm email"), so signup sends no confirmation mail and GoTrue returns a session immediately. Like the URL configuration above, this is dashboard-only state that the repo cannot enforce — read it back from the public `GET /auth/v1/settings` endpoint, where `mailer_autoconfirm: true` means confirmation is off. **`signupAction` does not assume either setting:** it branches on `data.session` from `supabase.auth.signUp` — session present → `revalidatePath` + `/dashboard`; session null → `/verify-email` as before. So flipping the toggle back on for production needs no code change, and `verify-email`/`resendConfirmationAction`/`auth.verifyEmail.*` are deliberately kept rather than deleted. Turning it back on is a launch item: with it off, anyone can register an address they don't own.
- **No rate limiting.** `/api/contracts/[id]/turn` triggers a billable LLM call and `src/proxy.ts`'s matcher excludes `/api` entirely (each route re-checks auth itself, by design — see the route's own header comment). The 2026-09-08 pass added a pre-flight `can_afford` credit gate, which stops a zero-balance workspace from burning free LLM calls, but a workspace *with* credits has no request-rate limit. Needs an infra decision (Vercel/Upstash) before it's addressed.